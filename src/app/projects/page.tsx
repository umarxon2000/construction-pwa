"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { Plus, MapPin } from "lucide-react";
import { useAppStore } from "@/lib/store";
const money=(n:number)=>new Intl.NumberFormat("uz-UZ").format(n)+" so'm";
export default function ProjectsPage(){const {projects,expenses,fetchAll,addProject}=useAppStore();const [open,setOpen]=useState(false);useEffect(()=>{fetchAll()},[fetchAll]);const save=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await addProject({clientName:String(f.get("clientName")),location:String(f.get("location")),clientPhone:String(f.get("clientPhone")),totalEarned:Number(f.get("totalEarned")),advances:0,contractType:f.get("contractType") as "labor_only"|"labor_and_materials"});e.currentTarget.reset();setOpen(false)};return <><div className="mb-5 flex justify-between"><div><p className="text-sm text-zinc-500">Barcha qurilish obyektlari</p><h1 className="text-2xl font-bold">Loyihalar</h1></div><button onClick={()=>setOpen(!open)} className="flex items-center gap-1 rounded-xl bg-zinc-950 px-3 py-2 text-sm font-semibold text-[#FFD700] dark:border dark:border-[#FFD700]/50"><Plus size={17}/>Yangi</button></div>{open&&<form onSubmit={save} className="mb-5 grid gap-3 rounded-2xl border border-zinc-200 bg-white p-4 dark:border-[#FFD700]/25 dark:bg-zinc-950"><input required name="clientName" placeholder="Mijoz ismi, masalan: Ali Valiyev" className="field"/><input required name="location" placeholder="Obyekt manzili" className="field"/><input required name="clientPhone" placeholder="Mijoz telefon raqami" className="field"/><input required min="0" name="totalEarned" type="number" placeholder="Mijoz bilan kelishilgan jami summa" className="field"/><select name="contractType" className="field"><option value="labor_only">Faqat ish haqi — materialni mijoz oladi</option><option value="labor_and_materials">Ish haqi + material — materialni biz olamiz</option></select><button className="rounded-xl bg-[#FFD700] py-3 font-bold text-black">Loyihani saqlash</button></form>}<div className="grid gap-3">{projects.filter(p=>p.status==="active").map(p=>{const spent=expenses.filter(e=>e.projectId===p.id).reduce((s,e)=>s+e.amount,0);return <Link href={`/projects/${p.id}`} key={p.id} className="rounded-2xl border border-zinc-200 bg-white p-4 dark:border-[#C0C0C0]/25 dark:bg-zinc-950"><div className="flex justify-between"><b>{p.clientName}</b><b className="text-amber-600 dark:text-[#FFD700]">{money(p.totalEarned-spent)}</b></div><p className="mt-2 flex gap-1 text-sm text-zinc-500"><MapPin size={14}/>{p.location}</p><p className="mt-2 text-xs text-zinc-500">{p.contractType==="labor_and_materials"?"Ish haqi + material":"Faqat ish haqi"}</p></Link>})}{!projects.filter(p=>p.status==="active").length&&<p className="text-zinc-500">Faol loyiha yo‘q.</p>}</div></>}